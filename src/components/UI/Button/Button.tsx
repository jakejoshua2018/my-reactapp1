import React from 'react';
import * as BootstrapButton from 'react-bootstrap';


const Button = (props: any) => {

  return (
    <BootstrapButton.Button variant="info">Info</BootstrapButton.Button>
  );
}

export default Button;
